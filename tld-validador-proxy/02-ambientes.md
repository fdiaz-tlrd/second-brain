# Ambientes

## Confirmado (fuente: usuario, este hilo)

| Ambiente   | Notas                                      |
|------------|--------------------------------------------|
| sandbox    | Pruebas; hay reportes de temas             |
| qa         | Pruebas; hay reportes de temas             |
| desarrollo | Solo el usuario desarrolla y prueba aquí   |

## No confirmado

- Diferencias de comportamiento de timeout entre ambientes más allá de `UrlCaTelered` en `samconfig.toml` (mismos `HTTP_READ_TIME_OUT` y timeout Lambda en `template.yaml` para todos los perfiles).
