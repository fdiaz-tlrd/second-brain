{
  "nombre": "2.5.7 matriz (N,∅) emisor ops N + validador sin ops (482)",
  "expectedHttpStatus": 400,
  "expectedCodigoError": 482,
  "expectedTipo": "general",
  "algoritmoCifrado": "aes-256-cbc",
  "body": {
    "idCanal": "{{CANAL_EMISOR_SIN_METODO}}",
    "validador": "{{CANAL_VALIDADOR_SIN_OPERACION}}",
    "peticion": {
      "idPeticion": "{{SWIFT_CANAL_EMISOR_SIN_METODO}}{{$timestamp}}",
      "metodo": "0001",
      "solicitudes": [
        {
          "idSolicitud": "1",
          "parametros": {
            "cuenta": "{{CUENTA_VALIDA}}"
          }
        }
      ]
    }
  }
}
