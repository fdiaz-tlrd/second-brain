# Run Newman — `5_fallosIntegracionValidador` (2026-07-06)

| Campo | Valor |
|-------|-------|
| Comando | `node run-newman.js vcn --folder "Metodo/0001/5_fallosIntegracionValidador"` |
| Fecha UTC | 2026-07-06T06:11:02Z |
| Resultado | **42 assertions, 13 fallos** (29 OK) |
| Historial | [`logs/historial/vcn/2026-07-06T06-11-02Z_Metodo-0001-5_fallosIntegracionValidador.json`](../../../../logs/historial/vcn/2026-07-06T06-11-02Z_Metodo-0001-5_fallosIntegracionValidador.json) |
| Resumen | [`logs/resumen-fallos-vcn.md`](../../../../logs/resumen-fallos-vcn.md) |

Referencias código VCN: `tld-api-cuenta-nombre/lambdas/cuenta-nombre/app.js` (L513–528 error proxy), `lib/catalogoRespuestas.js` (`MSG_CATALOGO`: 406, 500; **sin** 509 ni 599), `lib/util.js` (`lambdaResult`: HTTP 200 + `{codigoError,mensajeError}` en errores de negocio).

Proxy (vía homologado): mensajes catálogo en `tld-validador-proxy/lambdas/proxy/app.js` — 406, 509 `"Error inesperado en el Canal Validador"`, 599 `"Tiempo de espera agotado al llamar al Canal Validador"`.

Newman aserta textos de `ensamblador/catalogoGeneral.json` (406, 509, 599).

---

## Tabla por escenario

| # | Escenario | Newman espera | Recibido (VPN) | Código fuente VCN (intención) | Qué hacer |
|---|-----------|---------------|----------------|-------------------------------|-----------|
| 1 | **1022** demora → **599** | HTTP 200; `599`; `"Error inesperado en validador"` | **HTTP 502** (~13,1 s); body API GW `{"message":"Internal server error"}`; sin `codigoError` | Si proxy devuelve 599: `lambdaResult(200, 599, mensajeProxy)` — mensaje **texto proxy**, no `mensajeErrorCanal` (L518–528). Catálogo VCN no define 599. | Revisar **timeout** cadena API GW / lambda VCN / proxy vs demora dummy 16 s. Objetivo: respuesta negocio 200+599, no 502. Luego alinear mensaje (VCN catálogo vs proxy vs Newman). |
| 2 | **1022** cifrado invertido → **406** | HTTP 200; `406`; `"Error en descifrado canal validador"` | **OK** — coincide | Proxy 406 → VCN reenvía `message` proxy (L513–528). Coincide con `MSG_CATALOGO[406]` y Newman. | Nada en este run. |
| 3 | **1022** sin `respuesta` → **509** | HTTP 200; `509`; `"Error inesperado en validador"` | HTTP 200; `509`; `"Error inesperado en el Canal Validador"` | Proxy 509 → VCN pasa `respProxy.message` (L518–521), **no** el texto fijo de L537–549 ni catálogo Newman. | Decidir contrato único: VCN usar `mensajeErrorCanal` en L528, o actualizar catálogo Newman/proxy, o ambos. |
| 4 | **1023** demora → **599** | HTTP 200; `599`; catálogo Newman | **HTTP 500**; `500` / `"Error interno"` (cifrado en `respuesta`) | `lambdaResult(500, 500, …)` cuando falla `cerrarPaquete` u otra ruta 500 (p. ej. `app.js` L61–70). No es rama proxy 599. | **Canal 1023 (auth token)** no llega al fallo simulado: revisar Dynamo `OUTFGATO`, URL `/validar-fallos-con-token`, matriz token, deploy dummy. CloudWatch proxy/VCN. |
| 5 | **1023** cifrado invertido → **406** | HTTP 200; `406` | **HTTP 500**; `500` / `"Error interno"` | Idem — error interno antes de homologar 406. | Mismo bloqueo que fila 4; no probar 406/509 en 1023 hasta que 1023 responda como 1022. |
| 6 | **1023** sin `respuesta` → **509** | HTTP 200; `509` | **HTTP 500**; `500` / `"Error interno"` | Idem. | Mismo bloqueo que fila 4. |

---

## Resumen ejecutivo

| Bloque | Escenarios | OK Newman | Problema principal |
|--------|------------|-----------|-------------------|
| **1022_fijo** (PROXGATO) | 3 | **1/3** (solo 406) | Demora: **502** infra, no 599. 509: **código OK**, mensaje proxy ≠ catálogo Newman. |
| **1023_token** (OUTFGATO) | 3 | **0/3** | **500 Error interno** — auth/ruta token; no replica el camino del dummy. |

---

## Desalineación mensajes (si el código HTTP ya es 200)

| Código | Proxy devuelve | Newman / `catalogoGeneral` | VCN `app.js` L528 |
|--------|----------------|----------------------------|-------------------|
| 406 | Error en descifrado canal validador | Igual | Pasa texto proxy → **OK en run** |
| 509 | Error inesperado en **el Canal Validador** | Error inesperado en **validador** | Pasa texto proxy → **falló 1022.3** |
| 599 | Tiempo de espera agotado… | Error inesperado en validador | Pasa texto proxy (si no hay 502) |

---

## Próximo run sugerido

1. Solo `1022_fijo` tras ajustar timeout o `CFG_DELAY_MS` para confirmar 599 end-to-end.
2. Corregir **1023** en Dynamo + dummy antes de re-ejecutar token.
3. Tras cambios en VCN o catálogo: `git add logs/` y actualizar este archivo con nueva fila en [`logs/registro-vcn.md`](../../../../logs/registro-vcn.md).
