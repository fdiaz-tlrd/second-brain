# Logs de ejecución Newman

Salida generada por `run-newman.js` (no commitear salvo que quieras compartir un run concreto).

| Archivo | Contenido |
|---------|-----------|
| `ultimo-run-<suite>.json` | Reporte Newman completo |
| `resumen-fallos-<suite>.md` | Solo fallos — **compartir con el agente** |

`<suite>` = `p2m` | `p2p` | `vcn`
